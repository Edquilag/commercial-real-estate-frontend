# Leave blank when the API is served from the same origin (recommended).
VITE_API_URL=
VITE_PROPERTIES_ENDPOINT=/api/properties
# Origin of the Laravel sign-in and broker pages. Blank uses the current origin.
VITE_BACKEND_URL=http://localhost:8000
